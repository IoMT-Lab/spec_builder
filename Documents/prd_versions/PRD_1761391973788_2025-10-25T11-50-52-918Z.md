<!-- PRD snapshot (merged) for session 1761391973788 createdAt: 2025-10-25T11:50:52.918Z -->

# PRD for Photo Detector I²C Driver – Unit Tests & Symbol Discovery

Executive Summary

- Project Overview
  - Objective: Establish a comprehensive verification strategy for the PD_* I²C driver (AS7341 sensor interface) implemented on an STM32WB0x-based platform. The driver supports register reads/writes and LED on/off control, with a testing approach comprising host-based unit tests (Unity/CMock with a mocked I²C layer) and on-target integration tests (UART-based hardware harness exercising the full PD_* driver sequence on the embedded board with real sensor timing). Tests are integrated into CI (GitHub Actions) to flash firmware via OpenOCD/pyOCD, capture UART logs, and produce JUnit XML results.
  - Target Hardware and Driver: STM32WB0x (STM32WB05/STM32WB09) board running the PD_* driver for the AS7341 photo detector sensor.
  - Testing Approach:
    - Unit Tests: host-based, deterministic, using Unity/CMock with a mocked I²C backend to validate core driver logic (register semantics, LED state management, data path ingress/egress).
    - On-Target Tests: end-to-end integration on the STM32WB0x board using UART harness to exercise readiness waits, ID checks, threshold configuration, interrupt polling, and measurement validity with real sensor timing.
  - Success Criteria: ≥90% unit-test coverage for the driver module; reliable on-target integration sequences; deterministic CI results; well-documented test vectors and harness usage.
  - Deliverables: Reusable test harness, versioned test data sets, CI workflow (GitHub Actions) with OpenOCD/pyOCD flashing, UART log capture, and JUnit XML reporting; Symbol Discovery artifacts to anchor test automation to the actual PD_* API surface.

- Objectives
  - Deliver a maintainable, deterministic test suite that validates:
    - I²C operations and register semantics
    - LED on/off control behavior and interaction with other registers
    - Sensor data path conversions and data-ready signaling
    - End-to-end PD_* driver sequencing on hardware (readiness, ID, thresholds, interrupts, measurements)
  - Achieve target coverage (≥90% for unit tests) and reliable CI runs with reproducible vectors
  - Provide a clear, documented test harness, data sets, and guidance for extending tests as the driver evolves
  - Ensure reproducible build/test environments and accessible test artifacts in CI
  - Introduce a Symbol Discovery step to automatically enumerate PD_* API surface and bootstrap test stubs and CI anchors

Product Overview

- Features
  - Unit tests for: I²C register reads/writes, LED on/off control, sensor data path, data-ready signaling, and error handling (timeouts, NACKs, bus errors)
  - Mocked I²C layer for host-based tests to simulate bus conditions without hardware
  - On-target integration tests via UART-based hardware harness to exercise the full PD_* driver sequence on STM32WB0x with real sensor timing (readiness waits, ID checks, threshold configuration, interrupt polling, measurement validity)
  - Reproducible test vectors and data sets, with documentation on test design and conventions
  - CI integration (GitHub Actions) with firmware flashing (OpenOCD/pyOCD), UART log capture, and JUnit XML test reports
  - Symbol Discovery tooling to enumerate PD_* API symbols and generate test scaffolds and CI anchors

- User Personas
  - Software Engineer (Primary): Builds and maintains the driver and tests; relies on deterministic unit tests and clear test data
  - Quality Assurance Engineer: Validates coverage, reliability, and CI reports
  - DevOps/CI Engineer: Automates test execution, reporting, and firmware flashing in CI
  - Embedded Maintenance Engineer: Extends tests as the driver evolves; references the test harness and data sets

Requirements

- I²C Read/Write Correctness
  - Acceptance Criteria:
    - Read/write operations return correct data for representative registers (configuration, status, data registers)
    - I²C transaction sequencing (start, address, data, stop) is correct; ACK/NACK handling is respected
  - Test Conditions:
    - Mock I²C layer returns predefined data; verify driver interprets data correctly
    - Simulate ACK/NACK on reads/writes; exercise error paths and recovery

- LED Control
  - Acceptance Criteria:
    - LED on/off commands mutate the driver state and do not interfere with other register operations
    - Repeated LED commands are idempotent (no unintended toggling)
  - Test Conditions:
    - Issue LED on/off sequences and verify internal state via mocks and any side effects

- Sensor Data Path
  - Acceptance Criteria:
    - Raw sensor data reads convert to expected engineering units or data formats; data-ready signaling behaves as specified
  - Test Conditions:
    - Provide representative raw values; verify conversions per datasheet and boundary conditions

- Error Handling
  - Acceptance Criteria:
    - Timeouts, NACKs, and bus errors surface as controlled error codes/statuses
    - Driver remains stable and can recover after transient bus errors where applicable
  - Test Conditions:
    - Simulate timeouts, NACKs, and bus errors; verify error paths and post-error recovery

- Reset/Power-up
  - Acceptance Criteria:
    - After reset/power-up, registers reset to defaults, LED is off, and data path is stable
  - Test Conditions:
    - Trigger reset and verify post-reset state matches expectations

- On-target Integration Tests
  - Acceptance Criteria:
    - End-to-end PD_* driver sequence validated on hardware: readiness waits pass, ID checks succeed, thresholds configure, interrupts occur, and measurements are valid
  - Test Conditions:
    - Run on STM32WB0x board with real sensor timing; verify sequence correctness and measurement outputs

- CI & Test Reporting
  - Acceptance Criteria:
    - CI runs produce deterministic results with clear pass/fail signals; JUnit XML reports and test artifacts are generated
  - Test Conditions:
    - Configure GitHub Actions to flash firmware, capture logs, and publish results

- Test Environment & Data Management
  - Acceptance Criteria:
    - Host unit tests with Unity/CMock and on-target tests with UART harness share deterministic data sets; test data is versioned and documented
  - Test Conditions:
    - Provide repository structure for test data, mocks, and harness configurations

- Performance and CI Readiness
  - Acceptance Criteria:
    - Tests run within defined time windows; minimal flakiness; suitable for CI with stable results
  - Test Conditions:
    - Run across representative environments; monitor for flakiness across repeated executions

- Testing Strategy (Dedicated)
  - Unit Tests (Host)
    - Coverage: PD_* API surface (Init/Reset, ReadReg, WriteReg, LED_On, LED_Off, GetMeasurement) with mock I²C
    - Environment: Unity/CMock; deterministic vectors; mock I²C layer
    - Acceptance: ≥90% unit-test coverage; deterministic CI results; documented test vectors
  - Integration/On-Target Tests
    - Coverage: End-to-end PD_* driver sequence on STM32WB0x (readiness waits, ID checks, thresholds, interrupts, measurements)
    - Environment: UART-based hardware harness; real sensor timing
    - Acceptance: Pass/fail deterministic logs; clear sequence validation; CI visibility
  - CI & Data Management
    - Approach: GitHub Actions workflow to flash via OpenOCD/pyOCD, capture UART logs, publish JUnit XML results
    - Data: Versioned test data sets; harness configurations; reproducible vectors
  - Symbol Discovery (New)
    - Purpose: Scan the codebase to enumerate PD_* API symbols and auto-generate test stubs and CI anchors
    - Acceptance Criteria:
      - Discovery pass lists all detected PD_* API symbols (e.g., Init, ReadReg, WriteReg, LED_On, LED_Off, GetMeasurement)
      - Generated skeleton test stubs and CI references align with the actual code
    - Test Conditions:
      - Run discovery on the current codebase; produce a mapping document and skeletons; ensure buildable stubs

User Experience

- User Interface Design
  - The driver tests are developer-facing; no customer UI. A lightweight test harness and reporting UI (console output and CI dashboards) are provided. CI runs expose a test summary and per-test details via JUnit XML.
- User Flows
  - Not applicable to end-users; tests exercise code paths in isolation (unit tests) and end-to-end sequences on hardware (integration tests).
- Accessibility Requirements
  - Not applicable; ensure test code is readable, well-documented, and follows maintainability conventions.

Implementation

- Development Phases
  - Phase 0: Planning, Environment Setup, and Symbol Discovery
    - Activities: define test plan, vector strategy, harness wiring; implement/enable symbol discovery tooling; confirm hardware access for on-target tests
    - Deliverables: Symbol discovery tool, initial mapping document, skeleton test stubs, updated PRD references
  - Phase 1: Host Unit Test Framework
    - Activities: Set up Unity/CMock; implement I²C mock layer; configure test harness structure
  - Phase 2: I²C Read/Write and LED Tests
    - Activities: Implement unit tests for register access and LED control; verify sequencing and state
  - Phase 3: Sensor Data Path Tests
    - Activities: Implement tests for data conversion and data-ready signaling; edge/boundary tests
  - Phase 4: Error Handling Tests
    - Activities: Implement tests for timeouts, NACKs, and bus errors; verify recovery behavior
  - Phase 5: On-target Integration Harness Setup
    - Activities: Establish UART-based harness; define PD_* test sequences (readiness wait, ID check, thresholds, interrupts, validation)
  - Phase 6: CI Integration and Documentation
    - Activities: Wire tests to GitHub Actions; enable OpenOCD/pyOCD flashing; configure log capture and JUnit reporting; publish test documentation
- Dependencies
  - Unity/CMock (host unit tests)
  - Mock I²C library/framework compatible with the driver language
  - PD_* driver public APIs for reads/writes and LED control (exact symbol names to be finalized after code review)
  - UART-based hardware harness and access to the embedded board
  - STM32Cube/SDK support for OpenOCD/pyOCD integration
  - OpenOCD/pyOCD for on-target flashing
  - CI tooling for test execution and JUnit XML generation
  - Documentation tooling for test plans and data sets
  - Symbol Discovery tooling to enumerate PD_* symbols and generate test scaffolds
- Timeline & Milestones
  - Week 1: Finalize test plan, define target board specifics (STM32WB0x) and PD_* module scope; enable Symbol Discovery; set up host/unit testing framework
  - Weeks 2–3: Implement I²C read/write and LED unit tests; establish baseline coverage
  - Weeks 4–5: Implement sensor data path and error-handling tests
  - Week 6: Set up on-target integration harness and end-to-end test sequences on STM32WB0x
  - Week 7: Integrate with CI; implement test reporting (JUnit XML)
  - Week 8: Harden tests, finalize documentation, and conduct pilot runs
- Success Metrics
  - Achieve ≥90% code coverage for the driver module in unit tests
  - On-target integration tests pass in hardware runs with clear sequence validation
  - All tests pass reliably in CI with minimal flakiness
  - Clear, versioned test reports and data sets; maintainable test harness
  - Symbol Discovery outputs a reliable mapping of PD_* API symbols to test stubs with buildable scaffolds

Risk Assessment

- Technical Risks
  - Hardware timing differences could affect test determinism, especially on-target tests
    - Mitigation: Use deterministic mock timing for unit tests; define and enforce timeouts for on-target flows; decouple timing-sensitive code from logic where possible
  - Evolving register map or device features may render tests obsolete
    - Mitigation: Centralize register definitions; align tests with a single source of truth; update tests alongside driver changes
  - Flaky tests due to hardware initialization or UART logging issues
    - Mitigation: Stabilize harness boot sequence; add retry/logging guards; separate non-deterministic steps from core assertions
- Market/Adoption Risks
  - If hardware access is limited or varying hardware delays are encountered, end-to-end tests may be hard to maintain
    - Mitigation: Provide robust on-target test stubs or simulations; maintain a clear boundary between unit and integration tests; document hardware prerequisites
- Mitigation Strategies
  - Prioritize mocks and pure unit tests to minimize hardware variability
  - Maintain thorough test documentation and traceability to device datasheet registers
  - Automate test data generation and validation to prevent drift
  - Ensure CI includes reproducible environments and pinned tool versions

Notes

- This PRD focuses on unit testing for a photo detector I²C driver with LED control and on-target integration validation, augmented by a Symbol Discovery step to anchor test generation to the actual PD_* API surface. If additional driver features (e.g., interrupts, advanced power modes) are introduced, extend the test suite with corresponding requirements and tests in a future update.
- Exact PD_* symbol names will be finalized after code review; the Symbol Discovery step will auto-generate test stubs anchored to discovered symbols, with safe defaults (e.g., PD_Init, PD_ReadReg, PD_WriteReg, PD_LED_On, PD_LED_Off, PD_GetMeasurement) as fallbacks if necessary.